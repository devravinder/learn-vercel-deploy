{
  "version": 2,
  "env": {
    "APP_VERSION": "1.0.0"
  },
  "build": {
    "env": {
      "NODE_ENV": "production"
    }
  },
  "rewrites": [{ "source": "/(.*)", "destination": "dist/server.js" }],
  "installCommand": "pnpm install",
  "buildCommand": "pnpm run build"
}
